//! Show or update the hill chart. (Stub — see `docs/PRODUCT.md`.)

use crate::cli::{Cli, HillArgs};
use crate::error::Result;

/// Run the `hill` command.
pub fn run(args: &HillArgs, cli: &Cli) -> Result<i32> {
    super::unimplemented("hill", args, cli)
}
