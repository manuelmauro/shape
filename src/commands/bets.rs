//! List active and past bets. (Stub — see `docs/PRODUCT.md`.)

use crate::cli::Cli;
use crate::error::Result;

/// Run the `bets` command.
pub fn run(cli: &Cli) -> Result<i32> {
    super::unimplemented_bare("bets", cli)
}
