//! Work with pitches — the shaped artifact. (Stub — see `docs/PRODUCT.md`.)

use crate::cli::{Cli, PitchArgs};
use crate::error::Result;

/// Run the `pitch` command.
pub fn run(args: &PitchArgs, cli: &Cli) -> Result<i32> {
    super::unimplemented("pitch", args, cli)
}
