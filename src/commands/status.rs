//! Show the run dashboard: budget, scopes, hill, stuck dots. (Stub — see
//! `docs/PRODUCT.md`.)

use crate::cli::Cli;
use crate::error::Result;

/// Run the `status` command.
pub fn run(cli: &Cli) -> Result<i32> {
    super::unimplemented_bare("status", cli)
}
