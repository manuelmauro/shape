//! Mark the work done (deployed) and run the move-on checklist. (Stub — see
//! `docs/PRODUCT.md`.)

use crate::cli::Cli;
use crate::error::Result;

/// Run the `ship` command.
pub fn run(cli: &Cli) -> Result<i32> {
    super::unimplemented_bare("ship", cli)
}
